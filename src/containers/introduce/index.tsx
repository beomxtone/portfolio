import MusicPlayer from '../../components/MusicPlayer';
import Widget from '@/components/Widget';

const IntroducePage = () => {
  return (
    <Widget>
      <div>hey</div>
      <MusicPlayer duration={5} isFirst />
    </Widget>
  );
};

export default IntroducePage;
